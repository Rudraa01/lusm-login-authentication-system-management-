name: autheasy_flutter
description: Official Flutter SDK for AuthEasy - Drop-in authentication for any Flutter app (iOS, Android, Web, Desktop)
version: 1.0.0
homepage: https://autheasy.me
repository: https://github.com/Rudraa01/lusm-login-authentication-system-management-

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  shared_preferences: ^2.2.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
